resource_group_name  = "rg-enterprise-rag"
location             = "swedencentral"
storage_account_name = "stenterpriserag"
project_prefix       = "rag"

# PostgreSQL (required -- no default)
postgresql_admin_password = "your-secure-password"

# AI model defaults (override if needed)
# chat_model_name      = "Mistral-Large-3"
# embedding_model_name = "text-embedding-3-large"
# search_service_sku   = "basic"  (default, ~$75/month, required for semantic search)
# databricks_sku       = "premium"  (default)
